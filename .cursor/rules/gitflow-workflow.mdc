---
description: Mandatory GitFlow branching — feature/release/hotfix branches, develop integration, master releases
alwaysApply: true
---

# GitFlow workflow (mandatory)

Follow the [Atlassian GitFlow workflow](https://www.atlassian.com/git/tutorials/comparing-workflows/gitflow-workflow).
In this repo, GitFlow **`main`** is **`master`**. **`develop`** is the integration branch.

## Scope

GitFlow is mandatory for this coding repository. `neuriplo-platform` is the
architecture orchestrator (control plane) where GitFlow does not apply — work
there lands on `main`. See `neuriplo-platform/ops/policies.yaml`.

## Branch roles

| GitFlow branch | This repo | Purpose |
|----------------|-----------|---------|
| `main` | `master` | Official release history; tag every release here |
| `develop` | `develop` | Integration branch for completed features |
| `feature/*` / `feat/*` | `feature/*` / `feat/*` | New work; parent is always `develop` |
| `release/*` | `release/*` | Release prep (version, changelog, pins); no new features |
| `hotfix/*` | `hotfix/*` | Production patches; parent is always `master` |

Never commit feature work directly to `develop` or `master`. Never merge features
into `master`. Never fork feature branches from `master`.

## Agent Commit Signing

Every commit by an AI agent MUST include a `Co-authored-by` trailer with the
agent name, LLM model, and vendor email. See `AGENTS.md` "Agent Commit Signing"
for the full table of agents and format.

## Feature workflow

```bash
git checkout develop && git pull
git checkout -b feat/<short-name>          # or feature/<short-name>
# work, commit, push
# open PR: feat/<short-name> → develop
# after merge, delete the feature branch
```

## Release workflow

When `develop` is ready to ship:

```bash
git checkout develop && git pull
git checkout -b release/<version>          # e.g. release/0.5.0
# bump VERSION, CHANGELOG.md; run scripts/cut_release.sh <version>
# only release fixes/docs on this branch — no new features
git commit -m 'release: v<version>'
# PR or merge: release/<version> → master; tag v<version> on master
git checkout develop && git merge release/<version>
git branch -d release/<version>
git push origin --delete release/<version>
git push origin master develop --tags
# GitHub Release published by publish-github-release.yml after Release Guard
```

Release prep (`scripts/cut_release.sh`, `CHANGELOG.md`, sibling pins in
`versions.env`) happens on **`release/*`**, not on `develop` or `master`.

Pushing a tag with `git push --tags` does **not** create an entry on
https://github.com/olibartfast/neuriplo-infer/releases by itself — CI workflow
`.github/workflows/publish-github-release.yml` publishes the Release from
`CHANGELOG.md` after Release Guard succeeds on tag push.

## Hotfix workflow

```bash
git checkout master && git pull
git checkout -b hotfix/<short-name>
# fix, bump patch VERSION + CHANGELOG, commit
# merge hotfix → master; tag v<patch>
git checkout develop && git merge hotfix/<short-name>
git branch -d hotfix/<short-name>
git push origin --delete hotfix/<short-name>
git push origin master develop --tags
# GitHub Release published by publish-github-release.yml after Release Guard
```

## Before Commit, Push, or PR

1. Confirm the current branch matches the change type (feature / release / hotfix).
2. Confirm the target branch is allowed:
   - normal work → `develop` (or `feat/*` / `feature/*`)
   - release / hotfix → `master` via release/hotfix flow only
3. Never push normal development work directly to `master`.
4. Never force-push to `master`.
5. Run owning-repo validation (build/tests) before commit.
6. Only commit or push when the user explicitly asks.
7. Include `Co-Authored-By` agent/model trailer in commit body (see Agent Commit Signing above).

## Agent constraints

- Default base branch for new work: **`develop`**.
- PRs into **`master`** are release or hotfix merges only.
- Do not suggest trunk-based or `main`-only workflows for this repo.
- After any release or hotfix merge to `master`, always merge back into `develop`.
- Delete `feature/*`, `feat/*`, `release/*`, and `hotfix/*` branches locally **and on
  `origin`** after they are merged (see `gitflow-release-cleanup.mdc`).
